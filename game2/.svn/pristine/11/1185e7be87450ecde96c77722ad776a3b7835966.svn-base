package logic

import (
	//"encoding/json"
	//"fmt"
	//"reflect"
	"game2/service"
	"game2/lib/utils"
)

func UserInfoByRedis(token string) map[string]interface{} {
	//用户信息
	map_info, err := service.RedisGetMap(token)
	if err != nil {
		utils.Log(err, "debug", "")
		return nil
	}
	return map_info
}

